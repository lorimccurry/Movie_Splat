= link_to "Add a Movie", new_movie_entry_path
%p= current_user.username
%table.library
  %thead
    %tr
      %th My Rating
  %tbody
    %tr
      - @movie_entry.each do |movie_entry|
        %td= movie_entry.user_rating